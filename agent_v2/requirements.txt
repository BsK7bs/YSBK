# Runtime dependencies for agent_v2 (Windows).
# Kept small; native OS modules (pywin32, DPAPI) handle service + credential storage.
httpx==0.28.1
websockets==16.0
psutil==5.9.8
pydantic==2.13.4
python-dotenv==1.2.2
# Windows-only. Guarded by ``sys.platform == 'win32'`` where imported.
pywin32==306 ; sys_platform == "win32"
wmi==1.5.1 ; sys_platform == "win32"
pypiwin32==223 ; sys_platform == "win32"
